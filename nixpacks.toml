[phases.setup]
nixPkgs = ["python311", "python311Packages.pip", "python311Packages.pillow"]

[phases.install]
cmds = [
  "pip install --break-system-packages -r requirements.txt"
]

[phases.start]
cmd = "python bot.py"
